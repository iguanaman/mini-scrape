[project]
name = "mini-scrape"
version = "0.1.0"
description = "UK miniature wargaming price search"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "curl-cffi>=0.7",
    "selectolax>=0.3.21",
    "jinja2>=3.1",
]
